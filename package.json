{
	"name": "carbon-nexus",
	"private": true,
	"scripts": {
		"build": "turbo build",
		"dev": "turbo dev",
		"start": "turbo start",
		"test": "turbo test",
		"format": "bunx biome format --write ./*.{json,md,yml} && turbo biome:format",
		"lint": "bunx @biomejs/biome lint --write ./*.{json,md,yml} && turbo biome:lint"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.1",
		"turbo": "^2.1.1",
		"typescript": "^5.4.5"
	},
	"engines": {
		"node": ">=18"
	},
	"packageManager": "bun@1.1.26",
	"workspaces": ["apps/*", "packages/*"]
}
